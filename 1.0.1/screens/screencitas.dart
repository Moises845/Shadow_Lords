import 'package:flutter/material.dart';
class ScreenCitas extends StatelessWidget{
  const ScreenCitas ({Key? key}) : super( key : key);

  @override
  Widget build(BuildContext context){
    return const Center(
      child: Text("Citas")
    ); 
  }
}